version: "3"
services:
  reverseproxy:
    build:
      context: ./udagram-reverseproxy
    image: $YOUR_DOCKER_HUB/reverseproxy
  backend_user:
    build:
      context: ./udagram-api-user
    image: $YOUR_DOCKER_HUB/udagram-api-user
  backend_feed:
    build:
      context: ./udagram-api-feed
    image: $YOUR_DOCKER_HUB/udagram-api-feed
  frontend:
    build:
      context: ./udagram-frontend
    image: $YOUR_DOCKER_HUB/udagram-frontend